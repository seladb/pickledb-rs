sudo: false

language: rust

rust:
  - stable
  - beta
  - nightly

os:
  - linux
  - osx
  - windows

before_script:
  - rustup component add rustfmt clippy

script:
  - cargo fmt -- --check
  - cargo test
  - cargo clippy --all-targets
